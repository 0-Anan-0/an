# 多模态 RAG 工作台部署手册

## 1. 服务器初始化

### 1.1 更新系统
```bash
sudo apt update && sudo apt upgrade -y
```

### 1.2 安装必要工具
```bash
sudo apt install -y git curl wget build-essential
```

## 2. 后端部署

### 2.1 安装 Python 环境
```bash
# 安装 Python 3.12
sudo apt install -y python3.12 python3.12-venv python3-pip

# 创建虚拟环境
python3.12 -m venv langchain-env
source langchain-env/bin/activate
```

### 2.2 克隆项目
```bash
git clone <your-repository-url> langchain-env/an/LangChain_v1
cd langchain-env/an/LangChain_v1
```

### 2.3 安装后端依赖
```bash
# 安装必要的 Python 包
pip install fastapi uvicorn langchain langchain_text_splitters pymupdf Pillow pydantic python-multipart
```

### 2.4 启动后端服务
```bash
# 后台运行（使用 screen）
sudo apt install -y screen
screen -S backend -dm bash -c "cd ~/langchain-env/an/LangChain_v1 && source ~/langchain-env/bin/activate && python backend/src/main.py"
```

## 3. 前端部署

### 3.1 安装 Node.js
```bash
# 使用 nvm 安装 Node.js 18+
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 18
nvm use 18
```

### 3.2 安装前端依赖并构建
```bash
cd ~/langchain-env/an/LangChain_v1/frontend
npm install
npm run build
```

### 3.3 启动前端 SSR 服务器
```bash
# 后台运行（使用 screen）
screen -S frontend -dm bash -c "cd ~/langchain-env/an/LangChain_v1/frontend && npm run start"
```

## 4. Nginx 配置

### 4.1 安装 Nginx
```bash
sudo apt install -y nginx
```

### 4.2 配置 Nginx
```bash
# 创建 Nginx 配置文件
sudo tee /etc/nginx/sites-available/langchain << EOF
server {
    listen 80;
    server_name 47.109.155.196;

    # 前端 SSR 服务器代理
    location / {
        proxy_pass http://localhost:3000;  # 前端 SSR 服务器端口
        proxy_http_version 1.1;
        proxy_set_header Upgrade \$http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host \$host;
        proxy_cache_bypass \$http_upgrade;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade \$http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host \$host;
        proxy_cache_bypass \$http_upgrade;
    }
}
EOF

# 启用配置并重启 Nginx
sudo ln -s /etc/nginx/sites-available/langchain /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

## 5. 防火墙配置

### 5.1 开放必要端口
```bash
# 启用防火墙
sudo ufw enable

# 开放必要端口
sudo ufw allow 80/tcp      # 前端 Nginx
sudo ufw allow 3000/tcp     # 前端 SSR 服务器
sudo ufw allow 8000/tcp     # 后端 API
```

## 6. 服务状态检查

### 6.1 检查后端服务
```bash
# 查看后端服务状态
screen -r backend

# 测试后端 API
curl http://localhost:8000/docs
```

### 6.2 检查前端服务
```bash
# 查看前端服务状态
screen -r frontend

# 测试前端访问
curl http://localhost:3000
```

### 6.3 检查 Nginx 服务
```bash
# 检查 Nginx 状态
sudo systemctl status nginx

# 测试 Nginx 配置
sudo nginx -t
```

## 7. 公网访问验证

### 7.1 测试公网访问
在浏览器中访问：
- **前端应用**：`http://47.109.155.196`
- **后端 API 文档**：`http://47.109.155.196/api/docs`

### 7.2 功能测试
1. **文本聊天**：在前端输入文本消息，测试 AI 响应
2. **图像上传**：上传一张图片并提问，测试图像分析能力
3. **PDF 上传**：上传一份 PDF 文档并提问，测试基于文档的回答能力

## 8. 常见问题排查

### 8.1 后端服务无法启动
- **检查端口是否被占用**：`sudo lsof -i :8000`
- **检查依赖是否完整**：`pip list`
- **检查服务状态**：`screen -r backend`

### 8.2 前端无法访问
- **检查前端服务状态**：`screen -r frontend`
- **检查 Nginx 配置**：`sudo nginx -t`
- **检查防火墙规则**：`sudo ufw status`

### 8.3 前端无法连接后端 API
- **检查 CORS 配置**：确保后端允许前端域名
- **检查网络连接**：`curl http://localhost:8000/api/chat/stream`
- **检查 Nginx 代理配置**：确保 `/api/` 路径正确代理到后端

## 9. 监控与维护

### 9.1 设置日志监控
```bash
# 查看后端服务日志
sudo journalctl -u backend.service

# 查看 Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### 9.2 自动化备份
```bash
# 创建备份脚本
cat > ~/backup.sh << EOF
#!/bin/bash

# 创建备份目录
BACKUP_DIR=~/backups/$(date +%Y%m%d)
mkdir -p \$BACKUP_DIR

# 备份项目文件
cp -r ~/langchain-env/an/LangChain_v1 \$BACKUP_DIR/

# 备份配置文件
cp /etc/nginx/sites-available/langchain \$BACKUP_DIR/

echo "备份完成，备份文件位于：\$BACKUP_DIR"
EOF

# 添加执行权限
chmod +x ~/backup.sh

# 运行备份
~/backup.sh
```

### 9.3 定期更新
```bash
# 创建更新脚本
cat > ~/update.sh << EOF
#!/bin/bash

# 更新系统
sudo apt update && sudo apt upgrade -y

# 更新项目代码
cd ~/langchain-env/an/LangChain_v1
git pull

# 更新后端依赖
source ~/langchain-env/bin/activate
pip install --upgrade pip
pip install -r requirements.txt  # 如果有 requirements.txt 文件

# 重启服务
screen -S backend -X quit 2>/dev/null
screen -S frontend -X quit 2>/dev/null

screen -S backend -dm bash -c "cd ~/langchain-env/an/LangChain_v1 && source ~/langchain-env/bin/activate && python backend/src/main.py"
screen -S frontend -dm bash -c "cd ~/langchain-env/an/LangChain_v1/frontend && npm run start"

sudo systemctl restart nginx

echo "更新完成！"
EOF

# 添加执行权限
chmod +x ~/update.sh

# 运行更新
~/update.sh
```

## 10. 部署完成验证清单

- [ ] 后端服务在端口 8000 上运行
- [ ] 前端 SSR 服务器在端口 3000 上运行
- [ ] Nginx 在端口 80 上运行
- [ ] 公网 IP 可以访问前端应用
- [ ] 前端可以正常调用后端 API
- [ ] 所有模态功能（文本、图像、PDF）测试通过
- [ ] 流式响应正常工作
- [ ] 防火墙规则配置正确
- [ ] 备份和监控脚本已设置

## 11. 安全最佳实践

### 11.1 配置 HTTPS
```bash
# 安装 Let's Encrypt 证书
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

### 11.2 限制 API 访问
```bash
# 修改 Nginx 配置，添加 IP 白名单
sudo nano /etc/nginx/sites-available/langchain

# 在 location /api/ 块中添加
# allow 特定IP地址;
# deny all;
```

### 11.3 隐藏敏感信息
```bash
# 创建 .env 文件存储 API_KEY
cat > ~/langchain-env/an/LangChain_v1/.env << EOF
API_KEY=your-api-key-here
EOF

# 修改 main.py，从环境变量读取 API_KEY
```

---

本部署手册基于 Ubuntu 22.04 LTS 环境，包含了完整的多模态 RAG 工作台部署流程。根据实际服务器环境和项目需求，可能需要适当调整配置参数。